// AUTO-GENERATED by scripts/generate-config-hints.mjs from the kernel's
// config/default.toml. Do not edit — re-run `npm run generate:config-hints`.
// The REST API serves config values without documentation; these are the
// operator-facing descriptions for the Config page.

export const CONFIG_HINTS: Record<string, string> = {
  "agent_heartbeat.default_interval_secs": "Seconds between wakeups. 0 disables heartbeats globally (default).",
  "agent_heartbeat.jitter": "Jitter (0.0-1.0): lengthens each agent's interval by a deterministic per-agent fraction so a large idle fleet doesn't wake in lockstep.",
  "agent_heartbeat.max_wakes_per_tick": "Cap on agents woken per tick, to avoid an inference storm on a large idle fleet.",
  "api.config_writable": "Allow PUT /api/v1/config/{key} to write the config file at runtime. Off by default; enable only on trusted control-plane deployments.",
  "api.cors_allowed_origins": "Cross-origin allowlist for the REST API (full origins: scheme + host + port). Empty = same-origin only. Add your SPA dev origin, e.g. \"http://localhost:5173\".",
  "api.docs_enabled": "Serve the interactive API-docs UI at GET /api/v1/docs. The OpenAPI contract at GET /api/v1/openapi.json stays public regardless. Disable on public deployments.",
  "api.enabled": "REST API server. When enabled, the kernel starts an HTTP API alongside the Unix domain socket bus. Provides programmatic access to agents, tasks, tools, secrets, pipelines, audit, and notifications.",
  "api.operator_token": "Operator credential for POST /api/v1/auth/login. When unset/empty, login is disabled (503). Set this to let a browser SPA exchange it for a scoped, expiring key. Prefer an env-substituted value over committing a secret here.",
  "api.refresh_enabled": "Enable POST /api/v1/auth/refresh (rotate a valid key for a fresh one).",
  "audit.log_path": "Development default path (ephemeral on many systems).",
  "audit.max_audit_entries": "Maximum rows to retain (0 = unlimited). Older rows are pruned on each 10-minute sweep. Capped by default so the audit DB can't grow without bound; raise per deployment.",
  "audit.verify_last_n_entries": "Number of recent entries to verify on startup (0 = full chain, may be slow for large logs).",
  "bus.socket_path": "Development default socket path.",
  "chat.enforce_final_tag": "Enforce <final>...</final> tags on chat answers (off = stream everything the model emits outside tool-intent blocks).",
  "chat.max_tool_iterations": "Hard cap on tool-calling iterations per chat turn.",
  "chat.nudge_every_turns": "Every N user turns, inject a one-line system nudge asking the agent to persist durable learnings (facts / preferences / procedures). 0 disables.",
  "context_budget.chars_per_token": "Characters-per-token ratio for token estimation. Default 4.0 is accurate for English/Latin text. For CJK (Chinese/Japanese/Korean) workloads, use 1.5-2.0.",
  "context.summarization_max_input_chars": "Maximum characters of entry text sent to the summarizer LLM per compression event. Prevents sending enormous payloads on aggressive compression passes.",
  "context.summarization_mode": "Summarization strategy when context budget compression triggers. \"llm\" = LLM-generated summaries (best-effort, falls back to concat on failure) \"concat\" = concatenate entry snippets (legacy behavior) \"off\" = no summary entry — entries are silently evicted",
  "env.default_quota_bytes": "2 GiB (informational, not enforced)",
  "env.system_policy": "host packages go through host-package-install",
  "gateway.kind": "One [[gateway.channels]] table per bot. Example (uncomment + set credential_key): [[gateway.channels]]",
  "hal.raw_usb.allow": "Allowlist of raw-USB devices agents may open, as \"vid:pid\" hex strings. The raw-usb driver is fail-closed: with an empty list every open/read/write/control is denied. Requires the `raw-usb` build feature.",
  "kernel.autonomous_mode.task_timeout_secs": "24 hours",
  "kernel.autonomous_mode.tool_timeout_seconds": "10 minutes per tool call",
  "kernel.boot_replay_max_age_hours": "Queued tasks older than this are cancelled at boot instead of replayed, so a runaway backlog cannot resurrect itself on every restart. 0 disables.",
  "kernel.context_compaction.cadence": "Cadence in completed iterations: the compactor fires every N iterations (and only when there are enough compactable entries to produce a useful summary). Lower values compact more aggressively.",
  "kernel.context_compaction.enable_llm_summarization": "When true, the compactor calls the agent's LLM to generate a coherent semantic summary; on any LLM error it falls back to the extractive heuristic. Set false for tight-latency agents or unreliable local models.",
  "kernel.context_compaction.keep_recent_iterations": "How many recent iteration's worth of entries to keep verbatim. Older entries are merged into a rolling `[ROLLING TASK SUMMARY]` block.",
  "kernel.events.channel_capacity": "Capacity of the internal event dispatch channel (number of EventMessage slots). Events are dropped (with a warning logged and an EventChannelFull audit entry written) when the channel is full.  Increase this value if you observe dropped events under heavy load (10+ agents, frequent tool calls).",
  "kernel.failure_streak_limit": "Consecutive failures faster than failure_streak_fast_ms before an agent is auto-paused. 0 disables.",
  "kernel.health_bind": "Bind address for the health/metrics server. Loopback by default — /metrics exposes operational internals. Set \"0.0.0.0\" to expose beyond this host.",
  "kernel.health_port": "Port for the /healthz, /readyz, and /metrics HTTP endpoints (0 = disabled). Used by `agentos healthz` and Docker/k8s probes. Give each kernel instance a distinct port if you run more than one on the same host, or they will collide (the second instance logs a bind error and serves no health endpoint…",
  "kernel.max_concurrent_sandbox_children": "Maximum concurrent sandbox child processes. Default: number of logical CPUs (minimum 2). Increase if running many Community/Verified tools in parallel.",
  "kernel.max_concurrent_tasks": "Development defaults. Use config/production.toml for deployment.",
  "kernel.max_queued_per_agent": "max_concurrent_tasks bounds how many tasks RUN at once; these bound how many can pile up, how long a backlog survives, and when a failing agent is paused. Max queued (not running) tasks per agent. Past this, enqueues are rejected and marked Failed. 0 disables.",
  "kernel.sandbox_policy": "Sandbox execution policy for tool calls. \"trust_aware\" (default): Core tools run in-process, Community/Verified tools sandboxed. \"always\": All sandbox-eligible tools run in sandbox children (legacy behavior). \"never\": No sandboxing (development only, NOT for production).",
  "kernel.state_db_path": "SQLite DB for persisted runtime state (tasks, escalations, cost snapshots).",
  "kernel.task_retention_days": "Terminal scheduler rows older than this are deleted by the 10-minute sweep. 0 disables.",
  "kernel.tool_execution.default_timeout_seconds": "Timeout for in-process (non-sandboxed) tool calls. Sandboxed tools use their manifest's sandbox.max_cpu_ms instead, which is set per tool. Autonomous tasks use autonomous_mode.tool_timeout_seconds instead.",
  "kernel.tool_execution.max_output_bytes": "Maximum serialized bytes for a single tool's output injected into context. Outputs exceeding this are truncated with a marker so the agent can adapt. Long-running agentic workflows are not affected — only the output size is capped.",
  "llm.anthropic_base_url": "Reserved for provider endpoint documentation parity.",
  "llm.claude_code_resume": "Opt-in: let the `claude-code` adapter --resume the prior CLI session and send only the new turn's delta instead of replaying the full flattened context each call (lower latency/cost on long tasks). Off by default. Safe to enable: every resume is guarded by a prefix fingerprint that falls back to a…",
  "llm.custom_base_url": "Optional custom/OpenAI-compatible provider endpoint. For production, set AGENTOS_LLM_URL or configure this in config/production.toml.",
  "llm.max_tokens": "Maximum output tokens for Anthropic requests. Increase for long-form generation. Claude models support up to 8192 (claude-3) or 16384 (claude-3-5) output tokens.",
  "llm.ollama_context_window": "Context window size passed to Ollama as num_ctx. Many modern models support 32K+. Set to match your model's actual context size (e.g. 131072 for llama3.1:8b-instruct-q8_0).",
  "llm.openai_base_url": "Optional OpenAI base URL override (for gateways/proxies).",
  "llm.prompt_cache_ttl": "Anthropic prompt-cache TTL: \"5m\" (default) or \"1h\". Agents whose turns are more than 5 minutes apart (scheduled/heartbeat tasks) never get a cache read on the 5m TTL — they pay a cache write every turn. \"1h\" keeps the prefix warm across sparse turns; Anthropic bills 1h cache writes at a higher rate…",
  "llm.provider": "Optional provider fallback chain applied to every agent's primary adapter. When the primary provider errors (including mid-stream), the kernel fails over to these in order. Empty by default. Useful when the primary is a flaky free tier (e.g. NVIDIA) — fail over to a reliable local or paid model. NO…",
  "logging.log_dir": "Directory where rolling log files are written. Logs rotate daily; up to 7 days are kept. Set to \"\" to disable file logging (stderr only).",
  "logging.log_format": "Output format: \"text\" (human-readable, default) or \"json\" (structured, for log aggregators like Loki, Datadog, or Elasticsearch). In production, set to \"json\".",
  "logging.log_level": "Minimum log level: trace | debug | info | warn | error Can be overridden at runtime with the RUST_LOG environment variable or `agentos log set-level`.",
  "mcp.name": "List of MCP servers to connect at kernel boot. Each entry spawns a child process via stdio JSON-RPC. Imported tools are registered with TrustTier::Community and subject to full AgentOS capability-token and PermissionSet enforcement. Example: [[mcp.servers]]",
  "memory.background_review.enabled": "Post-task background review fork: after a non-trivial task or chat turn, an auxiliary inference pass extracts a reusable procedure, a context-memory patch, and/or durable facts, and writes them to the memory stores. Runs detached — never delays the reply.",
  "memory.background_review.min_tool_calls": "Review only tasks with at least this many tool calls (a task where the agent recovered from an error always qualifies).",
  "memory.context.enabled": "Per-agent self-curated context memory — injected into the context window at every task start AND every chat turn.",
  "memory.context.max_tokens": "Budget is deliberately tight: a hard cap is what forces the agent to curate (condense/replace) instead of appending prose forever. ~1k tokens ≈ 4k chars.",
  "memory.embedder_init_timeout_secs": "Max seconds for embedding-model init at boot (first boot downloads ~23 MB). On timeout/failure the kernel logs a warning and continues with a zero-vector embedder (semantic retrieval off, FTS5 lexical search unaffected) instead of hanging boot on a stalled download. Set disable_embedder = true to s…",
  "memory.lifecycle.reinforcement_enabled": "Reinforcement: retrieved memories are touched (last_used_at / use_count) on injection, and procedures injected into a task get success/failure feedback (Laplace-smoothed confidence recompute) when the task completes.",
  "memory.lifecycle.stale_after_days": "Curator lifecycle for procedures: unused for stale_after_days -> 'stale', for archive_after_days -> 'archived'. The curator never deletes; only the retention sweep prunes, and only archived rows. Procedures tagged \"pinned\" are exempt. Setting either to 0 disables the curator — procedure pruning the…",
  "memory.retention_days": "Age (days) after which episodic/semantic/procedural memory entries are swept. Bounds memory-DB growth on long-running installs. Set 0 for unbounded (old behavior).",
  "notifications.adapters.webhook.secret": "HMAC-SHA256 secret for X-AgentOS-Signature header (empty = no signature)",
  "notifications.max_inbox_size": "Maximum messages stored in the user inbox. When this limit is reached, the oldest read messages are purged on each write.",
  "notifications.notify_on_task_complete": "Automatically notify user when a root task completes successfully.",
  "notifications.notify_on_task_failed": "Automatically notify user when a root task fails.",
  "ollama.host": "Development default host. Override with AGENTOS_OLLAMA_HOST or production config.",
  "ollama.request_timeout_secs": "HTTP request timeout for Ollama inference calls (seconds). Cloud-proxied models (e.g. kimi-k2.5:cloud, gpt-oss:120b-cloud) and large local models with many tools in context may need 300–600s.",
  "personalization.dismiss_cooldown_hours": "Hours a dismissed recommendation's dedup_hash is suppressed (7d = 168h).",
  "personalization.enabled": "Proactive personalization. Tiered by token cost: L0 (read-back): inject a small, prompt-cached profile block into agent context. Costs ~20 tok/turn steady-state (cache read). L2 (background): interest model + proactive recommendations — ZERO task-context tokens (run off the task path). Master switc…",
  "personalization.interest_aggregation_trigger_hours": "...or after this many hours, whichever comes first.",
  "personalization.interest_aggregation_trigger_tasks": "Aggregate after this many task completions...",
  "personalization.interest_decay_half_life_hours": "Half-life in hours for interest-score decay (~2 weeks).",
  "personalization.interest_min_score": "Prune interest topics whose decayed score falls below this.",
  "personalization.max_recommendations_per_day": "Daily delivery cap.",
  "personalization.pin_rank_decay_half_life_days": "Exponential half-life (days) for profile pin_rank decay. After one half-life an unused entry's rank halves toward 0 (= higher L0 priority after boost).",
  "personalization.proactive_enabled": "Generate + deliver proactive tips. Off by default even when `enabled` is true.",
  "personalization.profile_archive_idle_days": "Archive Active profile entries idle for longer than this many days.",
  "personalization.profile_pin_cap": "L0 block bounds (also hard-capped in code at 8 / token budget).",
  "personalization.recommendation_dedup_cooldown_hours": "Hours before an identical (deduped) recommendation may repeat.",
  "personalization.recommendation_min_confidence": "Minimum confidence for a recommendation to be delivered.",
  "personalization.restate_confidence_boost": "Confidence bump when the user re-states an existing preference (clamped to 1.0).",
  "preflight.check_db_writable": "Probe that the audit/vault/state DB dirs, the log dir, and the bus-socket dir are writable before subsystem init.",
  "preflight.min_free_disk_mb": "Minimum free disk (MB) on the data-dir partition required to boot. Set to 0 to disable the disk check.",
  "registry.url": "Tool registry (marketplace) URL for `agentos tool search/add/publish`. Override with AGENTOS_REGISTRY environment variable for self-hosted registries.",
  "runtime.allowed_images": "Pre-approved Docker images. Agents can only use images on this list.",
  "runtime.backend": "Container runtime backend: \"docker\" (default) | \"none\" (disabled)",
  "scratchpad.auto_write_max_summary": "Maximum bytes for an auto-generated note.",
  "scratchpad.auto_write_min_steps": "Minimum episodic entries for a task to qualify (skip trivial tasks).",
  "scratchpad.auto_write_on_completion": "Auto-generate a scratchpad note when a task completes (success or failure).",
  "scratchpad.context_depth": "BFS traversal depth for context injection (0 = seed page only).",
  "scratchpad.enabled": "Agent scratchpad — graph-aware knowledge store for agent working memory.",
  "scratchpad.max_context_bytes": "Maximum total bytes of scratchpad content injected per inference call.",
  "scratchpad.max_context_pages": "Maximum pages injected into context per inference call.",
  "scratchpad.max_page_size": "Maximum content size per individual page (bytes).",
  "scratchpad.max_pages_per_agent": "Maximum pages per agent.",
  "secrets.vault_path": "Development default path (ephemeral on many systems). WARNING: /tmp is world-listable. For production use a private path such as ~/.agentos/vault/secrets.db or $XDG_DATA_HOME/agentos/vault/secrets.db. The kernel creates parent directories with 0o700 permissions at startup.",
  "skills.core_skills_dir": "Skill system directories. Skills are autonomous capability packages containing a SKILL.toml manifest + system prompt file.",
  "tools.core_tools_dir": "Development default paths (ephemeral on many systems).",
  "tools.discovery.default_scoping": "Scope the native tool array to task-relevant categories (frontier models). Anything scoped out is still reachable via search-tools. Set false to always send every tool. classifier: \"heuristic\" (default) | \"heuristic+semantic\" | \"llm\".",
  "tools.discovery.l0_max_names_per_category": "Tier-0 tool index shown in every system prompt: category counts plus this many usage-ranked tool names per category, soft-capped by l0_max_tokens.",
  "tools.discovery.rearm_on_describe": "Re-arm a scoped-out tool's native schema mid-task after the agent successfully calls describe-tool on it. Without this, category scoping is a hard wall for native tool calling (the model never receives the schema). Costs one tools-block cache bust per newly armed tool.",
  "tools.host_package.allowlist": "Operator-controlled allowlist. The agent can only install packages whose names match an entry verbatim, even after the user approves the call. Edit this list to expose more runtimes/tools to your agent fleet.",
  "tools.host_package.managers": "Package managers that the tool may invoke. The tool detects which is on PATH and uses the first match in this list.",
  "tools.host_package.privilege_escalator": "Privilege escalator policy: \"auto\" | \"pkexec\" | \"helper\" | \"none\". - auto: prefer pkexec, otherwise none. Recommended for desktops. - helper: invoke the setuid binary at `helper_path`. For headless servers. - none: disables the tool entirely (default if `enabled = false`).",
  "tools.workspace.allowed_paths": "Additional directories the agent can access beyond data_dir. Each entry must be an absolute path pointing to a project or shared directory. System roots (/etc, /var, /root, /home itself, /) are rejected at startup. Example: allowed_paths = [\"/home/user/project\", \"/shared/datasets\"] Empty by default…",
  "user_adaptation.enabled": "Deterministic post-task user preference proposal generator. Disabled by default; when enabled, proposals are queued for operator review.",
  "user_adaptation.proposal_ttl_days": "Pending proposals older than this are auto-expired (status='expired', history preserved) by the TimeoutChecker sweep.",
  "user_profile.db_path": "Optional SQLite path override; defaults to {data_dir}/user_profile.db.",
  "user_profile.enabled": "Structured user-profile store (proactive personalization). Accepted preference proposals are promoted here as durable, categorized entries that later phases read for L0 (pinned, in-context) and L1 (retrieved) tiers. The store itself is enabled by default (harmless when empty); the separate read-bac…",
  "user_profile.max_pinned": "Maximum L0-pinned entries surfaced to agent context.",
  "user_profile.min_confidence": "Minimum confidence (0.0..1.0) for an entry to be stored.",
  "web.auth_token": "Web UI authentication. The login token is resolved at startup in this order: 1. $AGENTOS_WEB_TOKEN environment variable (best for systemd/Docker — never written to disk). 2. auth_token set here. 3. A token generated once on first boot and persisted to {data_dir}/web_token (mode 0600), then reused o…",
};

/** Doc block above a `[table]` header — the fallback for its uncommented keys. */
export const CONFIG_SECTION_HINTS: Record<string, string> = {
  "agent_heartbeat": "Agent heartbeat — periodically wake idle agents to check their inbox and any assigned schedules, acting only when there is work. Opt-in: 0 disables it. This reuses the existing scheduler tick, agent inbox, and normal task path — it is not a second scheduler.",
  "approval": "Approval modes — when does the kernel auto-approve vs. escalate a tool call for human review? auto        — auto-approve everything except ControlPlane operations. ControlPlane (kernel admin: key rotation, audit truncation, agent shutdown) always prompts regardless of mode. ask_edit    — DEFAULT. A…",
  "env": "env-install: install packages into per-agent workspaces (NOT the host). Workspaces live at {data_dir}/workspaces/{agent_id}/{name}/ and are isolated per-agent. Network is permitted only for the install duration. Policy values: \"curated\" — package must be on the allowlist below (default) \"open\"    —…",
  "gateway": "Gateway (\"run as a bot\") — `agentos gateway run` boots the kernel and connects every channel listed here. Disabled by default. Tokens are referenced by a VAULT key (credential_key), never inline — seed with `agentos secret set <key> <token>`.",
  "hal.raw_usb": "Hardware Abstraction Layer policy.",
  "kernel.autonomous_mode": "Limits applied when a task is submitted with autonomous=true. These replace the complexity-based caps so long-running agents can work to natural completion without hitting artificial iteration or time ceilings.",
  "preflight": "Boot pre-flight checks — run before any subsystem starts so a misconfigured host fails fast with one precise diagnostic instead of crashing deep in init. (Dev defaults below; production profiles raise min_free_disk_mb.)",
  "runtime": "NOT YET WIRED — keys in [runtime] are parsed but currently IGNORED by the kernel. KernelConfig has no `runtime` field, so editing anything below has no effect yet (the container allowed_images list is hardcoded in kernel.rs). The block is retained to preserve design intent until the runtime backend…",
  "scheduler": "Scheduler run-history retention. The TimeoutChecker prunes completed/failed scheduled-run records older than this many days on its periodic sweep so the run store does not grow without bound. Set to 0 to disable pruning.",
  "tools.host_package": "host-package-install: install host OS packages via system package manager. DISABLED by default. The tool runs OUTSIDE the bwrap sandbox via `pkexec` or a setuid helper. Every call requires explicit user approval (control_plane risk class) AND the requested package must appear verbatim in `allowlist…",
  "transcription": "Inbound voice/audio transcription (speech-to-text) for channel media. When enabled, voice notes received on a channel (e.g. Telegram) are sent to an OpenAI-compatible /audio/transcriptions endpoint and the transcript is injected into the message the agent reads. Disabled by default. The API key is…",
};
