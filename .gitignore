node_modules
dist
dist-ssr
*.local
.env.local
.env.*.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# Test / build artifacts
coverage
playwright-report
test-results
.playwright-mcp/

# Python caches (skills)
__pycache__/
*.pyc

# Stray copies of the agos backend trees (config/*.toml, skills/core/*) and a
# saved API error dump. Nothing in src/ reads them; ignored so `git add -A`
# cannot commit backend files into the panel repo.
config/
skills/
agent-chats.json
