# Development against the Prism mock (npm run mock). Origin only — the OpenAPI
# paths already carry the /api/v1 prefix, so the base must NOT include it.
VITE_API_BASE=http://localhost:4010
VITE_WS_BASE=ws://localhost:4010
VITE_REFRESH_ENABLED=false
